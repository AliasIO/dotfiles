# Project Instructions

## Simulator Workflow

- After making iOS app code changes, rebuild and run the active simulator before handing work back so the installed app is ready for manual testing.
- When controlling the iOS Simulator UI, prefer the Computer Use plugin when possible; fall back to XcodeBuildMCP simulator taps/gestures only when Computer Use is unavailable or impractical.
