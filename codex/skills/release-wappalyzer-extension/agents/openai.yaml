interface:
  display_name: "Release Wappalyzer Extension"
  short_description: "Inspect, prepare, or publish Git releases"
  default_prompt: "Use $release-wappalyzer-extension to inspect release readiness first, then prepare locally or perform the Git release only if I explicitly request that mode."
