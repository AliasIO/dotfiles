interface:
  display_name: "Deploy Wappalyzer"
  short_description: "Propagate dependencies and deploy services"
  default_prompt: "Use $deploy-wappalyzer to inspect the rollout plan and dependency state without deploying until I explicitly authorize it."
